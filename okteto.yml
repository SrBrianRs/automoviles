build:
 srbrianrs:  # cambiar por su namespace de okteto
   image: okteto.dev/srbrianrs:latest  # cambiar por su namespace de okteto

deploy:
 - okteto build # build the image with the latest code
 - kubectl apply -f manifests # create all the resources in kubernetes
dev:
 srbrianrs:
   command: bash
   sync:
   - .:/automoviles   # cambiar por su proyecto